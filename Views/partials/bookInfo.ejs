<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= book.attributes.title %> - Profile</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Cinzel:wght@400;700&family=Poppins:wght@400;700&display=swap">
    <link rel="stylesheet" href="css/output.css">
    <style>
        .font-harrypotter {
    font-family: "Cinzel", serif !important;
}
    </style>
</head>
<body class="bg-gray-900 text-white p-6">

    <div class="container mx-auto">
        <h1 class="text-3xl text-center text-yellow-500 mb-6 font-harrypotter"><%= book.attributes.title %></h1>

        <div class="flex flex-col md:flex-row bg-gray-800 p-6 rounded-lg shadow-lg">
            <!-- Book Cover -->
            <div class="w-full md:w-1/3 flex justify-center">
                <img src="<%= book.attributes.cover %>" alt="Book Cover" class="rounded-lg ">
            </div>

            <!-- Book Details -->
            <div class="w-full md:w-2/3 md:pl-6 font-harrypotter p-6">
                <p><strong>Author:</strong> <%= book.attributes.author %></p>
                <p><strong>Release Date:</strong> <%= book.attributes.release_date %></p>
                <p><strong>Pages:</strong> <%= book.attributes.pages %></p>
                <p><strong>Summary:</strong> <%= book.attributes.summary %></p>
            </div>
        </div>

            <!-- Chapters -->
            <div class="mt-6 bg-gray-800 p-6 rounded-lg shadow-lg">
                <h2 class="text-xl font-bold mb-4">Chapters</h2>
                <div class="space-y-2">
                    <% chapters.forEach((chapter, index) => { %>
                        <button class="w-full text-left p-3 bg-gray-700 hover:bg-gray-600 rounded flex justify-between">
                            <span>Chapter <%= index + 1 %> - ID: <%= chapter.id %></span>
                        </button>
                    <% }); %>
                </div>
            </div>
    </div>

</body>
</html> 
 <!--The above code snippet is from the bookInfo.ejs file. It is a template file that is used to render the character profile page. The file contains HTML and EJS code that is used to display the details of a character in the Harry Potter universe. The file includes the character's name-->