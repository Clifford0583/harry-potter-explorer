<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= character.name %> - Profile</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Cinzel:wght@400;700&family=Poppins:wght@400;700&display=swap">
    <link rel="stylesheet" href="css/output.css">
</head>
<body class="bg-gray-900 text-white flex justify-center items-center min-h-screen p-4 font-body">

    <div class="container max-w-md sm:max-w-lg bg-gray-800 p-6 rounded-lg shadow-lg text-center">
        <!-- Character Name -->
        <h1 class="text-3xl  font-bold text-gray-200 border-b border-gray-700 pb-3 font-harrypotter"><%= character.attributes.name %></h1>

        <!-- Character Image -->
        <img class="w-40 h-40 rounded-lg mx-auto mt-4 shadow-md" 
            src="<%= character.attributes.image ? character.attributes.image : '/images/missing.png' %>" 
            alt="<%= character.attributes.name %>">

        <!-- Character Info -->
        <div class="mt-4 text-gray-300 space-y-2 text-lg">
            <p><strong class="text-gray-100 font-harrypotter">Gender:</strong> <%= character.attributes.gender || 'Unknown' %></p>
            <p><strong class="text-gray-100 font-harrypotter">House:</strong> <%= character.attributes.house || 'Unknown' %></p>
            <p><strong class="text-gray-100 font-harrypotter">Wand:</strong> <%= character.attributes.wands || 'Unknown' %></p>
            <p><strong class="text-gray-100 font-harrypotter">Patronus:</strong> <%= character.attributes.patronus || 'Unknown' %></p>
            <p><strong class="text-gray-100 font-harrypotter">Date of Birth:</strong> <%= character.attributes.born || 'Unknown' %></p>
        </div>

        <!-- Back Button -->
        <a href="/characters" class="inline-block mt-5 px-4 py-2 text-lg font-semibold text-white bg-blue-600 hover:bg-blue-700 rounded-lg shadow-md transition duration-300">
            ⬅ Back to All Characters
        </a>
    </div>

</body>
</html>